import React from "react";
import { Email_client } from "./emailClient";

const Email = async () => {
    
    return (
        <section id="email">
            <Email_client/>
        </section>
    );
}

export default Email;